import Foundation

enum AppWindowID: String {
    case main = "main-window"
    case player = "player-window"
    case plugin = "plugin-window"
    case caption = "caption-window"
    case programInfo = "program-info-window"
}
